# Runner Registration Tokens
# Generate via: gitea actions generate-runner-token --scope u128
# Generate via: gitea actions generate-runner-token --scope example-org
RUNNER_TOKEN_U128=
RUNNER_TOKEN_MPROJECTS=

# Gitea instance URL (internal)
GITEA_INSTANCE_URL=http://gitea-tailscale:3000
